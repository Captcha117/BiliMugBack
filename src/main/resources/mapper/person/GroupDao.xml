<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.oken1.modules.person.dao.GroupDao">
    <select id="getGroupConfigList" resultType="map">
        select g.*, n.nation_name_abbr region
        from mug_group g
                 left join tb_nation n on g.group_region = n.nation_code limit 100
    </select>
    <select id="searchGroup" resultType="map">
        select *, n.nation_name_abbr region
        from mug_group g
                 left join tb_nation n on g.group_region = n.nation_code
        where instr(g.group_name, #{search}) > 0
           or instr(g.group_alias, #{search}) > 0
    </select>
</mapper>