<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.oken1.modules.mug.dao.SongDao">
    <select id="getSongInfo" resultType="java.util.LinkedHashMap">
        SELECT
        s.song_id,
        s.game_id,
        g.common_name game_name,
        s.title,
        getSongSubName ( s.other_title ) other_title,
        s.artist,
        s.bpm,
        s.length,
        s.single_id,
        s.style_id,
        s.package_id,
        s.release_date
        FROM
        song s
        left join game g on s.game_id = g.game_id
        where
        song_id = #{songId}
    </select>
</mapper>